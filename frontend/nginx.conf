server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Serve frontend build files
    location / {
        try_files $uri /index.html;
    }

    # Proxy /upload to backend
    location /upload/ {
        proxy_pass http://backend:8000/upload/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Proxy /records to backend
    location /records/ {
        proxy_pass http://backend:8000/records/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Proxy /result to backend
    location /result/ {
        proxy_pass http://backend:8000/result/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
